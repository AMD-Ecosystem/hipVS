diff --git a/CMakeLists.txt b/CMakeLists.txt
index b80a5f4c9..c0199fd1a 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -41,9 +41,11 @@ if(FAISS_ENABLE_CUVS)
   include(rapids-export)
   include(rapids-find)

-  rapids_cuda_init_architectures(faiss)
-  rapids_cuda_init_architectures(pyfaiss)
-  rapids_cuda_init_architectures(faiss_c_library)
+  if(NOT FAISS_ENABLE_ROCM)
+     rapids_cuda_init_architectures(faiss)
+     rapids_cuda_init_architectures(pyfaiss)
+     rapids_cuda_init_architectures(faiss_c_library)
+  endif()
 endif()

 project(faiss
diff --git a/cmake/thirdparty/fetch_rapids.cmake b/cmake/thirdparty/fetch_rapids.cmake
index 4133a4460..2a2464bf8 100644
--- a/cmake/thirdparty/fetch_rapids.cmake
+++ b/cmake/thirdparty/fetch_rapids.cmake
@@ -18,10 +18,27 @@
 set(RAPIDS_VERSION "25.08")

 if(NOT EXISTS ${CMAKE_CURRENT_BINARY_DIR}/FAISS_RAPIDS.cmake)
-    if(NOT rapids-cmake-version)
-        set(rapids-cmake-version ${RAPIDS_VERSION})
+    if(FAISS_ENABLE_ROCM)
+        if(DEFINED ENV{RAPIDS_CMAKE_SCRIPT_REPO})
+            set(RAPIDS_CMAKE_SCRIPT_REPO "$ENV{RAPIDS_CMAKE_SCRIPT_REPO}")
+        else()
+            set(RAPIDS_CMAKE_SCRIPT_REPO ROCm-DS/ROCmDS-CMake)
+        endif()
+        if(DEFINED ENV{RAPIDS_CMAKE_SCRIPT_BRANCH})
+            set(RAPIDS_CMAKE_SCRIPT_BRANCH "$ENV{RAPIDS_CMAKE_SCRIPT_BRANCH}")
+        else()
+            set(RAPIDS_CMAKE_SCRIPT_BRANCH release/rocmds-25.10)
+        endif()
+        set(URL
+            "https://raw.githubusercontent.com/${RAPIDS_CMAKE_SCRIPT_REPO}/${RAPIDS_CMAKE_SCRIPT_BRANCH}/RAPIDS.cmake"
+        )
+        file(DOWNLOAD ${URL} ${CMAKE_CURRENT_BINARY_DIR}/FAISS_RAPIDS.cmake)
+    else()
+        if(NOT rapids-cmake-version)
+            set(rapids-cmake-version ${RAPIDS_VERSION})
+        endif()
+        file(DOWNLOAD https://raw.githubusercontent.com/rapidsai/rapids-cmake/branch-${RAPIDS_VERSION}/RAPIDS.cmake
+                ${CMAKE_CURRENT_BINARY_DIR}/FAISS_RAPIDS.cmake)
     endif()
-    file(DOWNLOAD https://raw.githubusercontent.com/rapidsai/rapids-cmake/branch-${RAPIDS_VERSION}/RAPIDS.cmake
-            ${CMAKE_CURRENT_BINARY_DIR}/FAISS_RAPIDS.cmake)
 endif()
 include(${CMAKE_CURRENT_BINARY_DIR}/FAISS_RAPIDS.cmake)
diff --git a/faiss/gpu/CMakeLists.txt b/faiss/gpu/CMakeLists.txt
index 50740450a..88fe84313 100644
--- a/faiss/gpu/CMakeLists.txt
+++ b/faiss/gpu/CMakeLists.txt
@@ -273,6 +273,10 @@ if(FAISS_ENABLE_CUVS)
           utils/CuvsUtils.cu)
 endif()

+if (FAISS_ENABLE_ROCM)
+    list(TRANSFORM FAISS_GPU_SRC REPLACE cu$ hip)
+endif()
+
 add_library(faiss_gpu_objs OBJECT ${FAISS_GPU_SRC})
 set_target_properties(faiss_gpu_objs PROPERTIES
   POSITION_INDEPENDENT_CODE ON
@@ -340,7 +344,7 @@ foreach(header ${FAISS_GPU_HEADERS})
 endforeach()

 if (FAISS_ENABLE_ROCM)
-  target_link_libraries(faiss_gpu_objs PRIVATE hip::host roc::hipblas)
+  target_link_libraries(faiss_gpu_objs PRIVATE hip::host roc::hipblas $<$<BOOL:${FAISS_ENABLE_CUVS}>:cuvs::cuvs>)
   target_compile_options(faiss_gpu_objs PRIVATE)
 else()
   # Prepares a host linker script and enables host linker to support
diff --git a/faiss/gpu/utils/MatrixMult-inl.cuh b/faiss/gpu/utils/MatrixMult-inl.cuh
index b28ea56da..ae035715f 100644
--- a/faiss/gpu/utils/MatrixMult-inl.cuh
+++ b/faiss/gpu/utils/MatrixMult-inl.cuh
@@ -24,17 +24,17 @@ struct GetCudaType;

 template <>
 struct GetCudaType<float> {
-    static constexpr hipblasDatatype_t Type = HIPBLAS_R_32F;
+    static constexpr hipDataType Type = HIPBLAS_R_32F;
 };

 template <>
 struct GetCudaType<half> {
-    static constexpr hipblasDatatype_t Type = HIPBLAS_R_16F;
+    static constexpr hipDataType Type = HIPBLAS_R_16F;
 };

 template <>
 struct GetCudaType<__hip_bfloat16> {
-    static constexpr hipblasDatatype_t Type = HIPBLAS_R_16B;
+    static constexpr hipDataType Type = HIPBLAS_R_16B;
 };

 #else
@@ -94,7 +94,7 @@ cublasStatus_t rawGemm(
             C,
             HIPBLAS_R_32F,
             ldc,
-            HIPBLAS_R_32F,
+            HIPBLAS_COMPUTE_32F,
             HIPBLAS_GEMM_DEFAULT);
 #else

@@ -199,7 +199,7 @@ cublasStatus_t rawBatchGemm(
             ldc,
             strideC,
             batchCount,
-            HIPBLAS_R_32F,
+            HIPBLAS_COMPUTE_32F,
             HIPBLAS_GEMM_DEFAULT);
 #else
     return cublasGemmStridedBatchedEx(
diff --git a/tests/CMakeLists.txt b/tests/CMakeLists.txt
index 285b9090e..d8eeeee38 100644
--- a/tests/CMakeLists.txt
+++ b/tests/CMakeLists.txt
@@ -1,5 +1,4 @@
-# Copyright (c) Meta Platforms, Inc. and affiliates.
-#
+
 # This source code is licensed under the MIT license found in the
 # LICENSE file in the root directory of this source tree.

@@ -80,11 +79,12 @@ endif()

 find_package(OpenMP REQUIRED)
 find_package(GTest CONFIG REQUIRED)
-
+find_package(Python3 REQUIRED COMPONENTS Development)
 target_link_libraries(faiss_test PRIVATE
   OpenMP::OpenMP_CXX
   GTest::gtest_main
   $<$<BOOL:${FAISS_ENABLE_ROCM}>:hip::host>
+  Python3::Python
 )

 # Defines `gtest_discover_tests()`.
