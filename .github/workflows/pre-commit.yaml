name: Pre-commit Checks

on:
  pull_request:
    branches:
      - amd-integration
  workflow_dispatch:

jobs:
  pre-commit:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          clean: true  # Run git clean -ffdx and git reset --hard HEAD before checkout

      - name: Set up virtual environment
        run: |
          rm -rf .venv || true
          python3 -m venv .venv
          source .venv/bin/activate
          python -m pip install --upgrade pip
          pip install pre-commit

      - name: Run pre-commit on all files
        env:
          SKIP: amd-copyright # This hook runs on all files including docs_amd/. Re-enable this once license_verificator has the ability to skip these files.
          RAPIDS_CMAKE_FORMAT_FILE: ${{ github.workspace }}/cmake-format-rapids-cmake.json
        run: |
          source .venv/bin/activate
          pre-commit run --all-files --show-diff-on-failure --color=always

      - name: Show pre-commit version
        if: always()
        run: |
          source .venv/bin/activate
          pre-commit --version
